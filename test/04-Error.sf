// Incorrect program to test errors
// Illegal endline after "to" in for loop

BEGINPROG Error
VARIABLES number, result 
  READ(number)
  result := (3 + 4 * 2
  IF (number >= 0) THEN
    FOR i := 1 TO 
      number DO				//Error
      result := result * i
    ENDFOR
    PRINT(result)
  ELSE      
    PRINT(-1)
  ENDIF

ENDPROG

